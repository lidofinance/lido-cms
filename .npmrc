registry=https://registry.npmjs.org/
# Disable sending metrics
send-metrics=false
# Always check SSL (important for security)
strict-ssl=true
# Disable creating package-lock.json
package-lock=false
# Network timeout in milliseconds (10 minutes)
fetch-timeout=600000
# Disable automatic script execution during package installation to protect against supply-chain attacks
ignore-scripts=true
